federation_version: =2.10.0

subgraphs:
  worforce_analytics:
      routing_url: https://workforce-graphql-3c62d5ab2e32.herokuapp.com/graphql  # <- can be omitted if the same as introspection URL
      schema:
        introspection_headers:  # Optional headers to include in introspection request
        #  Authorization: Bearer ${env.PEOPLE_AUTH_TOKEN}
